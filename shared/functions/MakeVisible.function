@default = "makeVisible"
definition {

	@summary-ignore = "true"
	function makeVisible {
		WaitForSPARefresh();

		selenium.waitForElementPresent();

		selenium.mouseOver();

		selenium.makeVisible();
	}

}