definition {
	var supportURL = PropsUtil.get("support.liferay.sandbox.url");

	@prose = "I go to the License Management page on sandbox"
	macro gotoLicenseManagementPage {
		Open(locator1 = "${supportURL}/group/license");
	}
}