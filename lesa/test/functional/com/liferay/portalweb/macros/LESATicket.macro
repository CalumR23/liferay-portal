<definition>
	<command name="assertTabNotSelected">
		<var name="key_tabName" value="${tabName}" />

		<execute function="AssertElementPresent" locator1="LESATicketDetails#TICKET_TAB" />
		<execute function="AssertElementNotPresent" locator1="LESATicketDetails#SELECTED_TAB" />
	</command>

	<command name="assertTabSelected">
		<var name="key_tabName" value="${tabName}" />

		<execute function="AssertTextEquals" locator1="LESATicketDetails#SELECTED_TAB" value1="${tabName}" />
	</command>

	<command name="createTicketBasicForm" returns="ticketNumber">
		<execute macro="Navigation#gotoLESA" />

		<execute macro="LESATicketCreate#inputBasicForm">
			<var name="appServer" value="${appServer}" />
			<var name="browser" value="${browser}" />
			<var name="component" value="${component}" />
			<var name="configType" value="${configType}" />
			<var name="database" value="${database}" />
			<var name="description" value="${description}" />
			<var name="javaVM" value="${javaVM}" />
			<var name="liferayVersion" value="${liferayVersion}" />
			<var name="operatingSystem" value="${operatingSystem}" />
			<var name="product" value="${product}" />
			<var name="project" value="${project}" />
			<var name="savedEnvConfigName" value="${savedEnvConfigName}" />
			<var name="server" value="${server}" />
			<var name="subcomponent" value="${subcomponent}" />
			<var name="summary" value="${summary}" />
			<var name="systemStatus" value="${systemStatus}" />
		</execute>

		<execute macro="LESATicketCreate#createTicketAndGetTicketNumber">
			<return from="ticketNumber" name="ticketNumber" />
		</execute>

		<return name="ticketNumber" value="${ticketNumber}" />
	</command>
</definition>