@default = "assertAlertNotPresent"
definition {

	function assertAlertNotPresent(value1) {
		WaitForSPARefresh();

		selenium.assertAlertNotPresent();

		selenium.assertJavaScriptErrors("true");

		selenium.assertLiferayErrors();
	}

}