@default = "assertConsoleTextPresent"
definition {

	function assertConsoleTextPresent(value1) {
		WaitForSPARefresh();

		selenium.assertConsoleTextPresent(${value1});

		selenium.assertJavaScriptErrors(${value1});

		selenium.assertLiferayErrors();
	}

}