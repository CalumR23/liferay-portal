@default = "isTextNotEqual"
definition {

	function isNotText(locator1,value1) {
		WaitForSPARefresh();

		selenium.waitForElementPresent(${locator1}, ${value1});

		selenium.isNotText(${locator1}, ${value1});
	}

	function isNotValue(locator1,value1) {
		WaitForSPARefresh();

		selenium.waitForElementPresent(${locator1}, ${value1});

		selenium.isNotValue(${locator1}, ${value1});
	}

	function isTextNotEqual {
		WaitForSPARefresh();

		if (contains(${locator1}, "/input")) {
			IsTextNotEqual.isNotValue();
		}
		else {
			IsTextNotEqual.isNotText();
		}
	}

}