@default = "selectFrame"
definition {

	function selectFrame(locator1,value1) {
		if (contains(${locator1}, "/iframe")) {
			WaitForFrameLoad.WaitForFrameLoad();

			selenium.waitForVisible(${locator1}, ${value1});

			selenium.mouseOver(${locator1});

			selenium.selectFrame(${locator1});

			selenium.assertJavaScriptErrors(${value1});

			selenium.assertLiferayErrors();
		}
		else {
			SelectFrame.selectFrameTop();
		}

		WaitForSPARefresh();
	}

	function selectFrameNoLoading(locator1,value1) {
		if (contains(${locator1}, "/iframe")) {
			selenium.waitForVisible(${locator1}, ${value1});

			selenium.mouseOver(${locator1});

			selenium.selectFrame(${locator1});

			selenium.assertJavaScriptErrors(${value1});

			selenium.assertLiferayErrors();
		}
		else {
			SelectFrame.selectFrameTop();
		}

		WaitForSPARefresh();
	}

	function selectFrameTop(value1) {
		selenium.selectFrame("relative=top");

		selenium.assertJavaScriptErrors(${value1});

		selenium.assertLiferayErrors();

		WaitForSPARefresh();
	}

	function selectSecondFrame(locator1,value1) {
		if (contains(${locator1}, "/iframe")) {
			selenium.waitForVisible(${locator1}, ${value1});

			selenium.mouseOver(${locator1});

			selenium.selectFrame(${locator1});

			selenium.assertJavaScriptErrors(${value1});

			selenium.assertLiferayErrors();
		}
		else {
			SelectFrame.selectFrameTop();
		}

		WaitForSPARefresh();
	}

}