@default = "assertConsoleTextNotPresent"
definition {

	function assertConsoleTextNotPresent(value1) {
		WaitForSPARefresh();

		selenium.assertConsoleTextNotPresent(${value1});

		selenium.assertJavaScriptErrors(${value1});

		selenium.assertLiferayErrors();
	}

}