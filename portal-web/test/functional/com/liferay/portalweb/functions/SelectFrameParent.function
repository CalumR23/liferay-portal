@default = "selectFrameParent"
definition {

	function selectFrameParent(value1) {
		selenium.selectFrame("relative=parent");

		selenium.assertJavaScriptErrors("true");

		selenium.assertLiferayErrors();

		WaitForSPARefresh();
	}

}