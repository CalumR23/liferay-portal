@default = "sikuliAssertElementPresent"
definition {

	function sikuliAssertElementPresent(locator1,value1) {
		WaitForSPARefresh();

		selenium.sikuliAssertElementPresent(${locator1});

		selenium.assertJavaScriptErrors(${value1});

		selenium.assertLiferayErrors();
	}

}