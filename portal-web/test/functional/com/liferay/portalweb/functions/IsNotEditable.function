@default = "isNotEditable"
definition {

	function isNotEditable(locator1,value1) {
		WaitForSPARefresh();

		selenium.waitForElementPresent(${locator1}, ${value1});

		selenium.waitForVisible(${locator1}, ${value1});

		selenium.isNotEditable(${locator1});
	}

}