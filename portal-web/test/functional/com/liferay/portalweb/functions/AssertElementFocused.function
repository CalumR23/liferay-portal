@default = "assertElementFocused"
definition {

	function assertElementFocused(locator1,value1) {
		selenium.assertElementFocused(${locator1});

		selenium.assertJavaScriptErrors("true");

		selenium.assertLiferayErrors();
	}

}