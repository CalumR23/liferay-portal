@default = "downloadTempFile"
definition {

	function downloadTempFile(value1) {
		WaitForSPARefresh();

		selenium.pause(5000);

		selenium.assertJavaScriptErrors("true");

		selenium.assertLiferayErrors();
	}

}