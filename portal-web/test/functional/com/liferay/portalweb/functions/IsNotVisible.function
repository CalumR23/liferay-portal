@default = "isNotVisible"
definition {

	function isNotVisible(locator1,value1) {
		WaitForSPARefresh();

		selenium.waitForElementPresent(${locator1}, ${value1});

		selenium.isNotVisible(${locator1});
	}

}