@default = "close"
definition {

	function close(value1) {
		WaitForSPARefresh();

		selenium.selectWindow("null");

		selenium.selectFrame("relative=top");

		selenium.assertJavaScriptErrors(${value1});

		selenium.assertLiferayErrors();
	}

	function closeAndSelectWindow(value1) {
		WaitForSPARefresh();

		selenium.pause(1000);

		selenium.close();

		selenium.pause(5000);

		selenium.selectWindow(${value1});

		selenium.assertJavaScriptErrors(${value1});

		selenium.assertLiferayErrors();
	}

	function closeWindow(value1) {
		WaitForSPARefresh();

		selenium.pause(1000);

		selenium.close();

		selenium.selectWindow("null");

		selenium.selectFrame("relative=top");

		selenium.assertJavaScriptErrors(${value1});

		selenium.assertLiferayErrors();
	}

}