definition {

	macro launchAzorUat {
		var uatURL = PropsUtil.get("test.this.cosette.azor.portal.url.uat");

		Default.Open(locator1 = "${uatURL}");
	}

	macro launchBenicarUat {
		var uatURL = PropsUtil.get("test.this.cosette.benicar.portal.url.uat");

		Default.Open(locator1 = "${uatURL}");
	}

	macro launchTribenzorUat {
		var uatURL = PropsUtil.get("test.this.cosette.tribenzor.portal.url.uat");

		Default.Open(locator1 = "${uatURL}");
	}

	macro launchWelcholhcpUat {
		var uatURL = PropsUtil.get("test.this.cosette.welcholhcp.portal.url.uat");

		Default.Open(locator1 = "${uatURL}");
	}

	macro launchWelcholUat {
		var uatURL = PropsUtil.get("test.this.cosette.welchol.portal.url.uat");

		Default.Open(locator1 = "${uatURL}");
	}

}