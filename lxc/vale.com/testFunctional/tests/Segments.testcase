definition {

	property test.env = "uat";
	property testray.main.component.name = "Vale";

	setUp {
		Utils.launchUatLoginPage();

		Login.loginAsAdminUser();

		Utils.launchUatHomePage();

		Utils.acceptCookies();
	}

	// LXC Refactor: This test is blocked because we are waiting for a confirmation about the implementation of the segmentation in the site
	// LXC Refactor ticket: LRQA-78772
	// @description = "Automation ID: LRQA-78477 | Test Summary: Verify specific content is not shown to a certain country/region"
	// @priority = "5"

	//test ShowsExpectedContentInHomePage {
	//	task ("When the user selected 'Brazil' as his country") {
	//		Segments.selectCountry(countryName = "BRAZIL");
	//}
	//	task ("Then the page experience should be aligned with brazilian content") {
	//		Segments.viewCountry(countryName = "BRAZIL");
	//}
	//	task ("When the user selects another country that isn't 'Brazil'") {
	//		Segments.selectCountry(countryName = "AUSTRALIA");
	//}
	//	task ("Then the page content will be different from Brazilian content") {
	//		Segments.viewCountry(countryName = "AUSTRALIA");
	//}
	//}

	test thisIsAStub {
		task ("This stub is a workaround to send this testcase file with the test ShowsExpectedContentInHomePage commented, please remove it when the refactor is done") {

			// This stub is a workaround to send this testcase file with the test ShowsExpectedContentInHomePage commented, please remove it when the refactor is done

			Default.Refresh();
		}
	}

}