definition {

	macro goToAsk {
		var askURL = "https://uat.liferay.dev/ask";

		Default.Open(locator1 = "${askURL}");
	}

	macro goToBlogs {
		var blogsURL = "https://uat.liferay.dev/blogs";

		Default.Open(locator1 = "${blogsURL}");
	}

	macro goToChat {
		var chatURL = "https://uat.liferay.dev/chat";

		Default.Open(locator1 = "${chatURL}");
	}

	macro goToDev24 {
		var dev24URL = "https://uat.liferay.dev/twentyfour";

		Default.Open(locator1 = "${dev24URL}");
	}

	macro goToLogin {
		var loginURL = "https://uat.liferay.dev/c/portal/login";

		Default.Open(locator1 = "${loginURL}");
	}

	macro goToMeet {
		var meetURL = "https://uat.liferay.dev//meet";

		Default.Open(locator1 = "${meetURL}");
	}

	macro goToProjects {
		var projectsURL = "https://uat.liferay.dev//projects";

		Default.Open(locator1 = "${projectsURL}");
	}

}