definition {

	macro goToAsk {
		var askURL = "https://liferay.dev/web/guest/ask";

		Default.Open(locator1 = "${askURL}");
	}

	macro goToBlogs {
		var blogsURL = "https://liferay.dev/web/guest/blogs";

		Default.Open(locator1 = "${blogsURL}");
	}

	macro goToChat {
		var chatURL = "https://liferay.dev/web/guest/chat";

		Default.Open(locator1 = "${chatURL}");
	}

	macro goToDev24 {
		var dev24URL = "https://liferay.dev/web/guest/twentyfour";

		Default.Open(locator1 = "${dev24URL}");
	}

	macro goToLogin {
		var loginURL = "https://liferay.dev/c/portal/login";

		Default.Open(locator1 = "${loginURL}");
	}

	macro goToMeet {
		var meetURL = "https://liferay.dev/web/guest/meet";

		Default.Open(locator1 = "${meetURL}");
	}

	macro goToProjects {
		var projectsURL = "https://liferay.dev/web/guest/projects";

		Default.Open(locator1 = "${projectsURL}");
	}

}