definition {

	setUp {
		task ("Open Portal") {
			PetrobrasUtils.launchPortal();
		}

		task ("Login") {
			Login.signIn();
		}
	}

	@description = "Verify that when the user clicks on 'Ver tudo' he can see all available vídeos at the 'Galeria de vídeos' page"
	test VerTudoLinkRedirectToVideoGallery {
		task ("Given user click on 'Ver tudo' button in 'Videos' widget") {
			Default.Click(
				key_pageLink = "vídeos",
				key_widgetName = "vídeos em destaque",
				locator1 = "HomePage#WIDGET_LINK");

			Default.AssertElementPresent(
				key_pageTitle = "Vídeos",
				locator1 = "HomePage#PAGE_TITLE");
		}

		task ("Then the user is redirected to Videos page and more videos are displayed") {
			PetrobrasUtils.viewURL(partialUrl = "group/petrobras-intranet/v%C3%ADdeos");

			HighlightedVideos.findVideoByIndex(index = "1,2,3");
		}
	}

}