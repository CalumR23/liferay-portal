definition {

	@summary = "Verify if a video is present on page by index"
	macro findVideoByIndex {
		for (var key_index : list "${index}") {
			Default.AssertElementPresent(
				key_index = "${key_index}",
				locator1 = "Video#VIDEO_BY_INDEX");
		}
	}

}