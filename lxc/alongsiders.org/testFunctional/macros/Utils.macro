definition {

	@summary = "Launching UAT Home page"
	macro launchUatHomePage {
		var portal = PropsUtil.get("test.this.alongsiders.portal.url.uat");

		Default.Open(locator1 = "${portal}/home");

		Default.WaitForVisible(locator1 = "General#SITE_LOGO");
	}

}