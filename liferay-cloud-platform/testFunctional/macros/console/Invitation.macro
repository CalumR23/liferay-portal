definition {

	macro addTeamMember {
		InvitationAPI.sendInvitation(
			email = "${email}",
			projectId = "${projectId}",
			role = "${role}");

		InvitationAPI.acceptInvitation(
			email = "${email}",
			projectId = "${projectId}");
	}

}