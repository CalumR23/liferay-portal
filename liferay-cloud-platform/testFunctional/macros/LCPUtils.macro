definition {

	macro goToPage {
		var lcpURL = PropsUtil.get("lcp.url");
		var keyPage = "${page}";

		Open(locator1 = "${lcpURL}${keyPage}");
	}

	macro launchLCP {
		var lcpURL = PropsUtil.get("lcp.url");

		Default.Open(locator1 = "${lcpURL}");
	}

}