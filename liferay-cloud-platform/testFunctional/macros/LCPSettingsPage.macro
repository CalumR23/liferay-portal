definition {

	macro deleteEnvironment {
		LCPUtils.goToPage(category = "Settings");

		if (isSet(project)) {
			var tabName = "Delete Environments";
		}
		else {
			var tabName = "Delete Environment";
		}

		Default.Click(
			key_tabName = "${tabName}",
			locator1 = "LCPSettings#SETTINGS_TAB");

		Default.Click(locator1 = "LCPUtils#DANGER_BUTTON");

		for (var delete : list "access,domains,data") {
			Default.Check.toggleSwitch(
				key_checkboxName = "${delete}",
				locator1 = "LCPUtils#ANY_CHECKBOX");
		}

		Default.Click(locator1 = "LCPUtils#DANGER_BUTTON");
	}

}