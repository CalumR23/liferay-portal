definition {

	macro searchForApp {
		var key_appTitle = "${appTitle}";

		Type(
			locator1 = "Marketplace#SEARCH_FIELD",
			value1 = "${appTitle}");

		MPButton.search();

		AssertClick(
			locator1 = "Marketplace#APP_TITLE",
			value1 = "${appTitle}");
	}

}